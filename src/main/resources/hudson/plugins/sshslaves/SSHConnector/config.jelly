<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout"
         xmlns:t="/lib/hudson" xmlns:f="/lib/form" xmlns:c="/lib/credentials">
  <f:entry title="${%Credentials}" field="credentialsId">
      <c:select/>
  </f:entry>

  <f:advanced>
      <f:entry title="${%Port}" field="port">
          <f:textbox default="22" />
      </f:entry>

      <f:entry title="${%JavaPath}" field="javaPath">
          <f:textbox />
      </f:entry>

      <f:entry title="${%JVM Options}" field="jvmOptions">
          <f:textbox />
      </f:entry>

      <f:entry title="${%Prefix Start Slave Command}" field="prefixStartSlaveCmd">
          <f:textbox />
      </f:entry>

      <f:entry title="${%Suffix Start Slave Command}" field="suffixStartSlaveCmd">
          <f:textbox />
      </f:entry>

      <f:entry title="${%Connection Timeout in Seconds}" field="launchTimeoutSeconds">
          <f:textbox name="launchTimeoutSeconds" />
      </f:entry>

      <f:entry title="${%Command to run before connection attempt}" field="beforeConnectCmd">
          <f:textarea name="beforeConnectCmd" />
      </f:entry>

      <f:entry title="${%Command to run before disconnection}" field="beforeDisconnectCmd">
          <f:textarea name="beforeDisconnectCmd" />
      </f:entry>

  </f:advanced>
</j:jelly>
